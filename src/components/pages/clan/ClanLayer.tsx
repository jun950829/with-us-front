'use client';
import styled from 'styled-components';

export default function ClanLayer() {
  return <ClanLayerWrap></ClanLayerWrap>;
}

const ClanLayerWrap = styled.section`
  width: 100%;
  padding: 30px;
`;
